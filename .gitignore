node_modules/
dist/
build/
docs/.vitepress/cache/
docs/.vitepress/dist/
*.log
.DS_Store
Thumbs.db
nul

# --- Compiler build / generated artifacts ---
output/

# --- Local IDE & agent config (not part of the SBM standard) ---
.claude/
.codex/
.cursor/
.xano/
.github/agents/
.github/copilot-instructions.md

# --- Unrelated XanoScript scaffold (boilerplate contamination, not the SBM docs standard) ---
# Files remain on disk; ignored so `git status` stays meaningful and `git add -A` can't
# pull them into the docs repo. Remove or relocate to a separate repo when decided.
addons/
agents/
apis/
functions/
mcp_servers/
middlewares/
realtime/
tables/
tasks/
tools/
workflow_tests/
AGENTS.md
CLAUDE.md

# --- Old v0.x scratch notes / stray examples ---
LEVEL-UPDATE-v0.4-COMPLETE.md
MIGRATION-COMPLETE-v0.4-PHASE-1.md
MIGRATION-v0.3-to-v0.4.md
PROJECT-UPDATE-v0.4-COMPLETE.md
examples/
